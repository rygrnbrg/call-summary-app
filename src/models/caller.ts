export interface Caller {
    date,
    number,
    type,
    duration,
    new,
    cachedName,
    cachedNumberType,
    cachedNumberLabel,
    phoneAccountId,
    viaNumber,
    name, //label
    contact, //id
    photo,
    thumbPhoto
}
